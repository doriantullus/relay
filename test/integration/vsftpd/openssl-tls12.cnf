# Cap OpenSSL at TLS 1.2 process-wide for vsftpd: 3.0.3 segfaults on
# TLS 1.3 data connections (see Dockerfile).
openssl_conf = default_conf

[default_conf]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
MaxProtocol = TLSv1.2
